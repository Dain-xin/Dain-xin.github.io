---
title: 事务查询
date: 2022-06-19 17:12:51
categories:
- 数据库
- mysql
tag:
keywords:
description:
cover: 'https://blog-images-djx.oss-cn-hangzhou.aliyuncs.com/img/202206191737629.png'
top_img: 'https://blog-images-djx.oss-cn-hangzhou.aliyuncs.com/img/202206191737629.png'
---

[数据库事务处理](https://blog.csdn.net/King_weng/article/details/106507133)

# 事务理解：

访问操作数据库的数据、进行一系列连锁的处理，这些操作处理全部执行或者都不执行。不可分割的整体。

对数据库的数据项修改的数据，可以暂时不写入数据库，缓存起来，用户自己可以看到修改的预览，

但是真实没有修改数据库里面的内容，提交后，不可以在撤销操作，然后其他用户就可以访问修改后的数据。

 

# 事务由四大特性：

原子性：事务本质，不可分割，全执行/都不执行。

隔离性：事务之间不受彼此影响。

持久性：提交的事务永久保存，不可撤销。

一致性：事务之间执行顺序不可更改，执行结果一致。



# 事务的sql命令和事务处理。

![image-20220619173726577](https://blog-images-djx.oss-cn-hangzhou.aliyuncs.com/img/202206191737629.png)

注：

**set names gbk;** **更改字符集。**

**删除数据库、表的删除操作、修改表的操作：都不能事务处理命令，直接提交。**

![image-20220619174001427](https://blog-images-djx.oss-cn-hangzhou.aliyuncs.com/img/202206191740482.png)

# 事务原理图：

![image-20220619174054178](https://blog-images-djx.oss-cn-hangzhou.aliyuncs.com/img/202206191740282.png)

# 拓展存储引擎：

![image-20220619183542485](https://blog-images-djx.oss-cn-hangzhou.aliyuncs.com/img/202206191835532.png)

# 事务隔离级别

| **脏读**       | **一个事务读取到了另一个事务中尚未提交的数据**               |
| -------------- | ------------------------------------------------------------ |
| **不可重复读** | **一个事务中两次读取的数据**内容**不一致，要求的是一个事务中多次读取时数据是一致的，这 **是事务 **update** **时引发的问题** |
| **幻读**       | **一个事务中两次读取的数据的****数量**不一致，要求在一个事务多次读取的数据的数量是一致  **的，这是* **insert** **或** **delete** **时引发的问题** |

![image-20220619183655616](https://blog-images-djx.oss-cn-hangzhou.aliyuncs.com/img/202206191836675.png)

1. 隔离级别越高，性能越差，安全性越高。
2. **查询隔离级别    select     @@tx_isolation;**
3. 设置隔离级别    **set     global transaction isolation level** **级别字符串**;





# **理解：**

**1、Mysql隔离级别高。**

**2、第一级别：未提交可读，另一个事务也可以读取到，那么脏读/幻读/不可重复读** **都满足；没有开启事务的读取，不需要提交，就能读到修改的数据。**

**3、第二级别：提交可读，只有提交后另一个事务才可以读到，这就不满足脏读了；没有开启事务的，读取到的都是另一个修改事务开启前的数据，只有修改后在能读到。**

**4、第三级别：重复度，针对增删操作的限制，基本和上一级限制类似。**

**5、第四级别：都被限制。**