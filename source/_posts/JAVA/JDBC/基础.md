---
title: JDBC基础
date: 2022-06-19 18:54:51
categories:
- JAVA
- JDBC
tag:
top:
keywords:
description:
cover: 'https://blog-images-djx.oss-cn-hangzhou.aliyuncs.com/img/202206192044133.png'
top_img: 'https://blog-images-djx.oss-cn-hangzhou.aliyuncs.com/img/202206192044133.png'
---

# JDBC基础

![image-20220619204341959](https://blog-images-djx.oss-cn-hangzhou.aliyuncs.com/img/202206192043167.png)



![未命名图片](https://blog-images-djx.oss-cn-hangzhou.aliyuncs.com/img/202207030938100.png)

# 获取数据库连接

[JDBC数据库连接池](https://blog.csdn.net/weixin_43520450/article/details/107231118)      [JDBC 连接数据库](https://www.w3cschool.cn/jdbc/jre31myf.html)

![image-20220704092301821](https://blog-images-djx.oss-cn-hangzhou.aliyuncs.com/img/202207040923945.png)

## 连接方法

![image-20220704102808144](https://blog-images-djx.oss-cn-hangzhou.aliyuncs.com/img/202207041028517.png)

## statement（查询方法）

核心代码是DriverManager提供的静态方法getConnection()。

DriverManager会自动扫描classpath，找到所有的JDBC驱动，然后根据我们传入的URL自动挑选一个合适的驱动。

因为JDBC连接是一种昂贵的资源，所以使用后要及时释放，使用try (resource)来自动释放JDBC连接是一个好方法。

**JDBC查询的返回值总是**ResultSet **（结果集类）**；Before start of result set：注意要加  **rs.next() 的限制**

### 步骤

第一步，通过Connection提供的createStatement()方法创建一个Statement对象，用于执行一个查询；

第二步，执行Statement对象提供的executeQuery("SELECT * FROM students")并传入SQL语句，执行查询并获得返回的结果集，使用ResultSet来引用这个结果集；

第三步，反复调用ResultSet的next()方法并读取每一行结果。

### 完整代码

![image-20220705085736681](https://blog-images-djx.oss-cn-hangzhou.aliyuncs.com/img/202207050857742.png)

### 理解

1、Statment和ResultSet都是需要关闭的资源，因此嵌套使用try (resource)确保及时关闭；

2、rs.next()用于判断是否有下一行记录，如果有，将自动把当前行移动到下一行（一开始获得ResultSet时当前行不是第一行）；

**3、ResultSet获取列时，索引从1开始而不是0；**

**==注意：必须根据SELECT的列的对应位置来调用getLong(1)，getString(2)这些方法，否则对应位置的数据类型不对，将报错。==**

## **SQL注入问题**

使用Statement拼字符串非常容易引发SQL注入的问题，这是因为SQL参数往往是从方法参数传入的。

例如：

期待的SQL：

**SELECT** * **FROM user WHERE** login='bob' **AND** pass='1234'

但是，如果用户的输入是一个精心构造的字符串，就可以拼出意想不到的SQL，这个SQL也是正确的，但它查询的条件不是程序设计的意图。例如：

name = "bob' OR pass=", pass = " OR pass='"：

**SELECT** * **FROM user WHERE** login='bob' **OR** pass=' AND pass=' **OR** pass=''

这样的SQL也是正确的，但是不合规矩。

**解决方法是对字符串参数添加转义，避免修改原SQL语句。**

或者用：**PreparedStatement**

## **PreparedStatement类使用**

PreparedStatement可以完全避免SQL注入的问题，因为PreparedStatement始终使用?作为占位符，并且把数据连同SQL本身传给数据库，这样可以保证每次传给数据库的SQL语句是相同的，只是占位符的数据不同，还能高效利用数据库本身对查询的缓存。

![image-20220705094957303](https://blog-images-djx.oss-cn-hangzhou.aliyuncs.com/img/202207050949377.png)

### **注：**

**使用PreparedStatement和Statement稍有不同，必须首先调用setObject()** 

**设置每个占位符?的值，最后获取的仍然是ResultSet对象。**

## **ResultSetMetaData**

**用于获取关于** **ResultSet** **对象中列的类型和属性信息的对象**

![image-20220705095418428](https://blog-images-djx.oss-cn-hangzhou.aliyuncs.com/img/202207050954612.png)

**数据类型：**

使用JDBC的时候，我们需要在Java数据类型和SQL数据类型之间进行转换。

JDBC在java.sql.Types定义了一组常量来表示如何映射SQL数据类型

| **SQL数据类型** | **Java数据类型**          |
| --------------- | ------------------------- |
| BIT,  BOOL      | boolean                   |
| INTEGER         | int                       |
| BIGINT          | long                      |
| REAL            | float                     |
| FLOAT,  DOUBLE  | double                    |
| CHAR,  VARCHAR  | String                    |
| DECIMAL         | BigDecimal                |
| DATE            | java.sql.Date,  LocalDate |
| TIME            | java.sql.Time,  LocalTime |

注意：只有最新的JDBC驱动才支持LocalDate和LocalTime。

### ResultSet和ResultSetMetaData

####  **ResultSet** 

1、查询需要调用PreparedStatement 的 executeQuery() 方法，查询结果是一个ResultSet 对象

2、对象内容：其实就是一张表的数据，最前面有一个游标，

## 