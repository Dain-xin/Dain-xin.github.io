---
title: 泛型理解
date: 2022-06-17 14:16:37
categories:
- JAVA
- 高级
---

前言：泛型为了解决集合、类、方法、接口、中数据类型转化问题（**java.lang.ClassCastException**）和 集合中对数据类型没有限制。在JDK5 出现了泛型来规定集合的存储类型。两种使用方法：1、直接规定存储类型；存储时不符合就报错。2、规定未知类型，在在创建对象的时候指定数据类型。

 

# 泛型

1、使用好处：避免强转的麻烦；避免运行的时候包ClassCastException。

2、**集合中使用泛型：** 

eg：

Collection<String>list=newArrayList<>();

这就是泛型使用：当集合明确类型后，存放类型不一致就会编译报错 // 集合已经明确具体存放的元素类型，那么在使用迭代器的时候，迭代器也同样会知道具体遍历元 素类型，就不是使用Object类型。

3、**类中使用泛型** ：    **修饰符 class 类名****<****代表泛型的变量****>** **{ }**

eg：

class ArrayList<E>{

public boolean add(E e){}

public  Eget(int index){}

....

}

在该类被new的时候指定类型。

4、**方法中使用泛型**：  **修饰符**  **<代表泛型的变量>** **返回值类型 方法名(参数){ }**

**eg:**

```java
public<MVP> MVPshow2(MVP mvp){

return  mvp;

}
```

5、接口中使用泛型： 修饰符 interface接口名<代表泛型的变量> { }

eg：interface MyGenericInterface<E>{  }

 

**注：所有未知类型的泛型变量都是在实现类对象****new****对象的时候指定的数据类型。**

 

# 泛型通配符

[java泛型通配符详解 ](https://www.cnblogs.com/minikobe/p/11547220.html)

 

我们在定义泛型类，泛型方法，泛型接口的时候经常会碰见很多不同的通配符，

## **常用的 T，E，K，V，？** 

本质上这些个都是通配符，没啥区别，只不过是编码时的一种约定俗成的东西。比如上述代码中的 T ，我们可以换成 A-Z 之间的任何一个 字母都可以，并不会影响程序的正常运行，但是如果换成其他的字母代替 T ，在可读性上可能会弱一些。**通常情况下，T，E，K，V，？是这样约定的：**

- ？表示不确定的 java 类型？ 是无界通配符
- T (type) 表示具体的一个java类型
- K V (key value) 分别代表java键值中的Key Value
- E (element) 代表Element

 

## **上界通配符 < ? extends E>**

上界：用 extends 关键字声明，表示参数化的类型可能是所指定的类型，或者是此类型的子类。

在类型参数中使用 extends 表示这个泛型中的参数**必须是 E 或者 E 的子类**，这样有两个好处：

- 如果传入的类型不是 E 或者 E 的子类，编译不成功
- 泛型中可以使用 E 的方法，要不然还得强转成 E 才能使用

private <K extends A, E extends B> E test(K arg1, E arg2){

  E result = arg2;

  arg2.compareTo(arg1);

  //.....

  return result;

}

类型参数列表中如果有多个类型参数上限，用逗号分开

 

## **下界通配符 < ? super E>**

下界: 用 super 进行声明，表示参数化的类型可能是所指定的类型，或者是此**类型的父类型，直至 Object**

在类型参数中使用 super 表示这个泛型中的参数必须是 E 或者 E 的父类。

```java
private <T> void test(List<? super T> dst, List<T> src){

  for (T t : src) {

    dst.add(t);

  }

}

 

public static void main(String[] args) {

  List<Dog> dogs = new ArrayList<>();

  List<Animal> animals = new ArrayList<>();

  new Test3().test(animals,dogs);

}

// Dog 是 Animal 的子类

class Dog extends Animal {
}
```

dst 类型 “大于等于” src 的类型，这里的“大于等于”是指 dst 表示的范围比 src 要大，因此装得下 dst 的容器也就能装 src 。

 

**注：上界通配符主要用于读数据，下界通配符主要用于写数据。**

 

## **？和 T 的区别**

总结：T 是一个 确定的 类型，通常用于泛型类和泛型方法的定义，？是一个 不确定 的类型，通常用于泛型方法的调用代码和形参，不能用于定义类和泛型方法。

eg:

```java
// 通过 T 来 确保 泛型参数的一致性

public <T extends Number> void

test(List<T> dest, List<T> src)

 

//通配符是 不确定的，所以这个方法不能保证两个 List 具有相同的元素类型

public void

test(List<? extends Number> dest, List<? extends Number> src) 

//Class<T>和Class<?>

public static<T>TreString(T request,Class<T>clazz){

Tt=null;

t=(T)request;

returnt;

}

public static void main(String[] args){

Objecto=*reString*(1.0,Double.class);

System.*out*.println("o="+o);

System.*out*.println(o.getClass());

}
```

